<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plz.config.mybatis.mapper.skillMapper">
<!-- #########################################################################
				Select sql
	##########################################################################
 -->
<sql id="select-sql">
	SELECT	
	FROM	SKILL
	WHERE	
</sql>



<!-- #########################################################################
				데이터 수정 
	##########################################################################
 -->
<insert id="insertSkill" parameterType="skill">
	INSERT INTO SKILL
	VALUES	(
			#{email},
			#{codeSkill}
			)
</insert>

<delete ide="deleteSkill" parameterType="String">
	DELETE
	FROM	SKILL
	WHERE	email = #{email}
</delete>

<update id="updateSkill" parameterType="skill">
	UPDATE	SKILL
	SET		code_skill = #{codeSkill}
	WHERE	email = #{email}
</update>

<!-- #########################################################################
				데이터 조회 
	##########################################################################
 -->
<
</mapper>